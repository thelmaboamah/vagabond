 <aside class='col-md-5'>
    <ul class='profile-info list-unstyled text-center'>
      <li>
        <%= image_tag @user.image.url(:medium) %>
      </li>
      <li><%= @user.first_name + " " + @user.last_name %>
        <span><%= link_to "🖊", edit_user_path  %></span>
      </li>
      <li>📍 <%= @user.current_city %></li>
      <li>Memeber since: <%= @user.created_at.strftime("%m/%d/%Y") %></li>
      <li>Entries posted: <%= @user.entries.count %></li>
      <li>Business Role: <%= @user.business_role %></li>
      <li>Description: <%= @user.description %></li>
      <li><%= link_to "Add Entry", new_entry_path, class: "fake-btn" %></li>

    </ul>
  </aside>
  <div class="col-md-7 pull-right">
    <h2>Your entries <%= link_to "➕", new_entry_path %></h2>
    <% @user.entries.each do |entry| %>
      <div class="user-show-entries">
        <h4><%= entry.created_at.strftime("%m/%d/%Y") %> | <%= link_to entry.title, entry_show_path(entry.id) %></h4>
      </div>
    <% end %>
  </div>
